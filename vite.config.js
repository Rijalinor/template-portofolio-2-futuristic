import { defineConfig } from 'vite';

export default defineConfig({
  base: '/template-portofolio-2-futuristic/',
  root: './',
  build: {
    outDir: 'dist',
  },
  server: {
    open: true,
  },
});
